import { motion } from "framer-motion"; import styled from "styled-components"; export const StyledLineScroll = styled(motion.div)` display: flex; flex-direction: column; align-items: center; justify-content: center; position: fixed; bottom: 0; left: 2.5rem; z-index: 96; @media(max-width: 950px) { left: auto; right: 40px; bottom: -30px; } @media(max-width: 600px) { left: auto; right: 6.25%; bottom: -30px; } `; export const StyledLineScrollRoute = styled(motion.div)` display: flex; flex-direction: column; align-items: center; justify-content: center; position: fixed; bottom: 0; left: 2.5rem; z-index: 96; @media(max-width: 950px) { left: 6.25%; bottom: -30px; } @media(max-width: 600px) { left: 6.25%; bottom: -30px; } `; export const Text = styled(motion.p)` position: relative; font-size: .875rem; font-weight: 500; letter-spacing: 0.2rem; -webkit-writing-mode: vertical-rl; -ms-writing-mode: tb-rl; writing-mode: vertical-rl; color: var(--white); z-index: 96; text-transform: uppercase; font-family: var(--jost-light), sans-serif; white-space: nowrap; :after { content: ""; width: .125rem; height: 4rem; background: var(--white); position: absolute; left: 0; right: 0; margin: auto; bottom: -5rem; } @media(max-width: 950px) { font-size: 12px; } @media(max-width: 600px) { font-size: .625rem; } `; 