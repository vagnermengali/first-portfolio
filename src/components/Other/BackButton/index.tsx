import { useContext, useEffect } from "react"; import { GlobalContext } from "@/context/globalContext"; import { BackButtonProps } from "@/interfaces/BackButtonProps/BackButtonProps"; import { StyledBackButton } from "./style"; import Icon from "@/components/Other/Icon";const BackButton = ({ route }: BackButtonProps) => { const { setAnimationImage, setAnimationImageSlide, router, isMobile } = useContext(GlobalContext); useEffect(() => { const delaySlide = setTimeout(() => { setAnimationImageSlide(false); }, 1000); return () => clearTimeout(delaySlide); }, [setAnimationImageSlide]); return ( <> { router.pathname !== "/" && <StyledBackButton href={`/#${route}`} aria-label="return" whileHover={{ left: "2rem" }} onClick={() => { isMobile ? (router.push(`/#${route}`)) : ( setAnimationImage(false), setAnimationImageSlide(true), router.push(`/#${route}`) ) }} > <Icon className="imageBackButton" id="Arrow" width={"64"} height={"34"} onClick={() => { isMobile ? (router.push(`/#${route}`)) : ( setAnimationImage(false), setAnimationImageSlide(true), router.push(`/#${route}`) ) }} /> </StyledBackButton> } </> ) }; export default BackButton 