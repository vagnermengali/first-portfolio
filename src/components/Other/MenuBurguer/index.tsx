import { useContext } from "react"; import { GlobalContext } from "@/context/globalContext"; import { MenuAnimation, StyledMenuBurger } from "./style"; const MenuBurguer = () => { const { showSideBar, isSideBarVisible, isMobile } = useContext(GlobalContext); return ( <StyledMenuBurger onClick={showSideBar}> <MenuAnimation isSideBarVisible={isSideBarVisible} isMobile={isMobile} onClick={showSideBar} ></MenuAnimation> </StyledMenuBurger> ); }; export default MenuBurguer; 