import { motion } from "framer-motion"; import styled from "styled-components"; export const StyledHeader = styled(motion.header)` position: fixed; width: 100%; display: flex; justify-content: space-between; padding: 2.5rem 2.5rem 0; z-index: 99; .logo { height: 100%; color: var(--white); font-size: 1.5rem; font-family: var(--jost-light); letter-spacing: 0.1em; transition: 0.3s; text-decoration: none; :hover { color: var(--brand5) } } @media(max-width: 950px) { .logo { font-size: 1.5rem; } } @media(max-width: 600px) { padding: 1.25rem; .logo { font-size: 1rem; } } `; export const Navigation = styled(motion.div)` display: flex; align-items: center; justify-content: space-between; `; export const Icons = styled(motion.div)` display: flex; align-items: center; justify-content: center; margin-right: 6rem; gap: 1.875rem; @media(max-width: 750px) { display: none } `; export const Link = styled(motion.a)` height: 100%; color: var(--white); font-size: 1.5rem; font-family: var(--jost-light); letter-spacing: 0.1em; transition: 0.3s; text-decoration: none; :hover { color: var(--brand5) } `; export const IconLink = styled(motion.a)` display: flex; align-items: flex-start; color: var(--white); font-size: 1.625rem; line-height: 2.625rem; transition: 0.3s; text-decoration: none; text-align: start; vertical-align: top; margin: auto; :hover { color: var(--brand5) } `