import { motion } from "framer-motion"; import styled from "styled-components"; export const StyledSidebar = styled(motion.div)` position: fixed; background: var(--brand4); z-index: 98; transition: height .5s ease; width: 100vw; height: 100vh; display: flex; align-items: center; justify-content: center; flex-direction: column; overflow: hidden; `; export const Menu = styled(motion.ul)` list-style: none; width: fit-content; height: fit-content; display: flex; align-items: center; justify-content: space-between; flex-direction: column; position: absolute; display: flex; gap: 2.625rem; `; export const SelectSlide = styled(motion.a)` display: inline-block; color: var(--white); font-size: 5rem; line-height: 1; letter-spacing: 0.1em; font-weight: normal; text-decoration: none; text-transform: uppercase; white-space: nowrap; position: relative; :hover { :after { width: 100%; } } :after { content: ""; width: 0%; height: .6rem; background: var(--white); position: absolute; left: 0; top: 50%; margin: auto; transition: all .3s ease; } @media(max-width: 950px) { font-size: 3.375rem; :after { height: .3rem; } } @media(max-width: 600px) { font-size: 2.375rem; :after { height: .3rem; } } `; export const ContentIcon = styled(motion.div)` display: flex; gap: 1.4rem; `; export const IconLink = styled(motion.a)` display: flex; align-items: flex-start; color: var(--white); font-size: 1.4rem; line-height: 2.625rem; transition: 0.3s; text-decoration: none; text-align: start; vertical-align: top; margin: auto; @media(min-width: 600px) { font-size: 2rem; } `