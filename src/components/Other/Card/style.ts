import { motion } from 'framer-motion'; import styled from 'styled-components'; import { ImgProps } from '@/interfaces/TemplateSlideVerticalProps/TemplateSlideVerticalProps'; import { JustifyProps } from '@/interfaces/TemplateRouteProps/TemplateRouteProps'; export const Box = styled(motion.li) <ImgProps>` min-width: 20rem; width: 100%; height: 18rem; background-image: url(${props => props.image}); background-position: center; background-repeat: no-repeat; background-size: cover; position: relative; @media(max-width: 950px) { } @media(max-width: 600px) { min-width: 100%; height: 18rem; } `; export const Div = styled(motion.div)` position: absolute; width: 100%; height: 100%; padding: 1.5rem 2rem; z-index: 1; display: flex; justify-content: space-between; flex-direction: column; @media(max-width: 950px) { } @media(max-width: 600px) { padding: .8rem 1.1rem; } `; export const Title = styled(motion.h2)` font-size: calc(1em + 0.5vw); `; export const DivIcons = styled(motion.div) <JustifyProps>` display: flex; align-items: center; gap: .4rem; .icon { width: 1.7rem; height: 1.7rem; } @media(max-width: 950px) { } @media(max-width: 600px) { width: 50%; overflow: auto; justify-content: ${props => props.justify_content}; &::-webkit-scrollbar { width: 0rem; height: 0rem; } .icon { width: 1rem; height: 1rem; } } `; export const Icon = styled(motion.img)` width: 1.7rem; height: 1.7rem; `; export const Button = styled(motion.a)` width: 1.7rem; height: 1.7rem; color: var(--white); font-family: var(--futura-light); letter-spacing: 0.1em; transition: 0.3s; text-decoration: none; :hover { color: var(--brand5) } .icon { width: 1.7rem; height: 1.7rem; } `; export const DivButton = styled(motion.div)` display: flex; justify-content: space-between; gap: 1.125rem; @media(max-width: 950px) { } @media(max-width: 600px) { gap: 1rem; } `;